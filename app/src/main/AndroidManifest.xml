<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ Add INTERNET permission for Retrofit/API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker"
        android:usesCleartextTraffic="true"> <!-- ✅ Add this for HTTP traffic during development -->

        <!-- LoginActivity is the one and only LAUNCHER activity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup screen is internal only -->
        <activity
            android:name=".auth.SignupActivity"
            android:exported="false" />

        <!-- DetailExpense screen is internal only -->
        <activity
            android:name=".detail.DetailExpenseActivity"
            android:exported="false" />

        <!-- MainActivity is the main UI, but it is NOT a launcher. -->
        <!-- It should only be started by LoginActivity. -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:configChanges="orientation|keyboardHidden|screenSize" />

        <!-- NewCategoryActivity is internal only -->
        <activity
            android:name=".ui.NewCategoryActivity"
            android:exported="false" />

    </application>

</manifest>
